/*
 * Cross build configuration header (replaces the per-system <whoami.h>).
 *
 * We build the plain, non-overlay PDP-11 toolchain: MENLO_OVLY and the
 * other site-specific overlay/VM options are deliberately left undefined,
 * selecting the simplest authentic 2.8BSD code paths.
 */
